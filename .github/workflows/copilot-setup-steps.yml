name: Copilot Setup Steps

on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    runs-on: ubuntu-24.04

    permissions:
      contents: read

    services:
      redis:
        image: redis:8.4.0-bookworm

      serverless-redis-http:
        image: hiett/serverless-redis-http:0.0.10
        ports:
          - 8079:80
        env:
          SRH_MODE: env
          SRH_TOKEN: local_development_token
          SRH_CONNECTION_STRING: redis://redis:6379

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Install CJK fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk

      - name: Set up uv
        uses: astral-sh/setup-uv@ed21f2f24f8dd64503750218de024bcf64c7250a # v7.1.5
        with:
          version: '0.9.17'

      - name: Enable Corepack for pnpm
        run: |
          npm install -g corepack@0.31.0
          corepack enable

      - uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.1.0
        with:
          cache: pnpm
          node-version-file: .node-version

      - name: Install project dependencies
        run: pnpm install

      - name: Install Python dependencies for insights app
        run: |
          cd apps/insights
          uv sync --extra dev

      - name: Build internal packages to generate type definitions
        run: pnpm typegen

      - name: Set Next.js Turbopack TLS environment variable
        run: echo 'NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1' >> $GITHUB_ENV

      - name: Start Supabase services via Docker Compose
        run: docker compose up -d db kong mailpit
          
      - name: Wait for services to be healthy
        run: |
          echo "Waiting for database (60s timeout - initial startup may take time)..."
          timeout 60 bash -c 'until docker compose exec -T db pg_isready -U supabase_admin; do sleep 2; done'
          
          echo "Waiting for Kong API Gateway (60s timeout - depends on multiple services)..."
          timeout 60 bash -c 'until curl -f http://localhost:54321/health > /dev/null 2>&1; do sleep 2; done'
          
          echo "All services are healthy!"

      - name: Set environment variables for Supabase and Upstash
        run: |
          echo 'NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321' >> $GITHUB_ENV
          echo 'NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0' >> $GITHUB_ENV
          echo 'SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU' >> $GITHUB_ENV
          echo 'UPSTASH_REDIS_REST_TOKEN=local_development_token' >> $GITHUB_ENV
          echo 'UPSTASH_REDIS_REST_URL=http://localhost:8079' >> $GITHUB_ENV
